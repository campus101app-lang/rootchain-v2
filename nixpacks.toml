# Railway/Nixpacks — Node 22 for monorepo API build
[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build --workspace=@rootchain/server"]

[start]
cmd = "npm run start:prod --workspace=@rootchain/server"
